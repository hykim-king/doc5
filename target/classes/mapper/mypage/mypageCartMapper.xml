<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.doc5.mapper.MypageCartMapper">
	<select id="doSelectOne" parameterType="MypageCartVO" resultType="MypageCartVO">
	SELECT
	    c.*,
	    cgo.*
	FROM
	    doc5_cart c
	LEFT JOIN doc5_cart_goods_option cgo
	  ON c.seq = cgo.c_seq
    WHERE c.seq = #{seq}
	</select>
	
	<insert id="doSave">
	<selectKey keyProperty="seq" order="BEFORE" resultType="int">
		SELECT c_seq.NEXTVAL FROM dual
	</selectKey>
	INSERT INTO doc5_cart (
	    seq,
	    goods_no,
	    branch_code,
	    order_no,
	    user_id,
	    goods_name,
	    goods_price,
	    goods_cnt,
	    tumbler_fl,
	    hot_fl,
	    hot_price,
	    ice_fl,
	    ice_price,
	    cancel_dt,
	    reg_dt,
	    mod_dt
	) VALUES (
        #{seq},
        #{goodsNo},
        #{branchCode},
        #{orderNo},
        #{userId},
        #{goodsName},
        #{goodsPrice},
        #{goodsCnt},
        #{tumblerFl},
        #{hotFl},
        #{hotPrice},
        #{iceFl},
        #{icePrice},
        SYSDATE,
        SYSDATE,
        SYSDATE
    )
	</insert>
	
	<select id="getCount" resultType="int">
	SELECT COUNT(*) FROM doc5_cart
	</select>
	
	<delete id="deleteAll">
	DELETE FROM doc5_cart
	</delete>
</mapper>